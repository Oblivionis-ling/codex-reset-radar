# V2 runtime directory

Local V2 state is created here at runtime:

- data/codex-reset-radar-v2.db
- logs/*.jsonl
- pids/*.json

All runtime contents are ignored by Git. Only this policy file is versioned.
