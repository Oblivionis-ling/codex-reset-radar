# Analysis artifacts

This directory is reserved for small, sanitized, reviewable analysis artifacts. Generated migration candidates and real post corpora are ignored by default and require an explicit privacy and size review before they can be committed.
