# Codex Reset Radar 当前状态与 UNKNOWN 原因报告

检查时间：2026-09-21 17:03—17:06（北京时间）。本报告是本次只读检查的快照，不覆盖旧验收证据；后续自然调度可能改变数量与最新 Judge。

## 结论

服务没有掉线，数据库没有损坏，模型也不是没有产出。当前四个等级全部 UNKNOWN 的直接原因是父帖修复中引入的 **Judge 有效性校验字段不一致**：读取结果已经把 `raw_json` 转成 `raw`，校验仍读 `raw_json`，导致合法上下文版本信息丢失，结果被误判为无效。Backend 随后统一返回 UNKNOWN，Web 只是显示了这个返回值。

此缺陷在前一次诊断中已经发现，但尚未实施修复，当前运行的仍是同一代码指纹。不能将本次状态解释为新故障、正常预测不确定性或用户操作错误。

同时存在独立的采集健康问题：Profile / Replies 心跳过期约 24 小时 37 分钟，只有 Search 在持续上报。模型结果的真实数据健康为 STALE，不应在修复颜色显示后宣称采集全面正常。

## 当前运行状态

| 项目 | 实际结果 |
| --- | --- |
| 程序标识 | `2.0.0-alpha.4` + 未提交父帖修复，基线提交 `53575d0c64525b778dea28ebb919f8d9b1138e98` |
| 加载指纹 | `c9721c73c737ef2b`，与工作区核心文件指纹一致 |
| Backend | `http://127.0.0.1:8787`，PID 17904，2026-09-20 16:12:38 启动，仍运行 |
| Web | `http://127.0.0.1:5173`，PID 15204，2026-09-20 16:12:42 启动，仍运行 |
| Web 数据 | 本地 Web 代理与 Backend 返回相同的 Judge 229 / invalid / UNKNOWN；未进行本轮浏览器视觉核验 |
| 模型 | `deepseek-v4-flash`，现有真实业务调用路径 |
| Prompt | 分析 `v2-post-semantics-9-context`；翻译 `v2-zh-translation-2-context`；Judge `v2-reset-judge-7-context` |
| 数据库 | 397 条帖子、14 条正式事件、3 条候选、13 条周期、238 条分析、229 条 Judge |
| 数据库检查 | `integrity_check=ok`，外键错误 0 |
| 最新完整 Reset | 2026-09-12 08:09:17 UTC，即北京时间 16:09:17；未因 Banked 预告提前推进周期 |
| 工作树 | 父帖修复和先前启动器修改仍未提交；未发布新版本 |

`health.status=healthy` 只说明服务响应正常，不能据此认定完整业务链正常。

## UNKNOWN 的具体证据

最新 Judge：`229`，数据库状态 `COMPLETED`。

- 判断时间：2026-09-21 16:12:53 北京时间。
- 处理完成：16:14:02。
- 有效期截至：18:12:53，检查时尚未过期。
- 原始模型等级：主等级 YELLOW、24h YELLOW、48h ORANGE、72h ORANGE。
- 原始数据健康：STALE。
- 保存了 24 条 `input_versions`，全部与当前语义输入哈希匹配。
- 引用与当前 `judge_evidence` 禁用名单交集为空。

读取逻辑位于 `apps/backend/app/db.py:1297`：从结果移除 `raw_json`，解析为 `raw`。校验逻辑位于同文件 `1312`：仍从 `raw_json` 获取版本信息。因此进入“没有输入版本”的兼容分支，遇到已管理上下文的回复证据后返回 False。

只读诊断中对相同对象做了内存对照：

| 对照 | 结果 |
| --- | --- |
| 现有返回对象直接校验 | False |
| 仅在内存把已存在的 `raw` 提供给校验所读字段 | True |

未修改模型答案、数据库、内容政策或任何生产状态。这证明当前 UNKNOWN 的直接原因是字段契约错误，而非最新输入失效。

`apps/backend/app/main.py` 收到 False 后，在 137—141 行将主等级、三个窗口和数据健康全部替换为 UNKNOWN。其提示“当前尚无已完成的模型判断”也不准确：真实情况是有结果，但被校验错误拒绝。

这些原始颜色只是诊断证据，不代表已通过业务语义审核。Judge 229 将同帖的 Banked / 模糊周二语境作为完整 Reset 长窗口升色依据，仍需要与既有 Full / Special 边界核对，不能用修复显示代替语义验证。

## 采集与父帖状态

| 来源 | 最后心跳（北京时间） | 判断 |
| --- | --- | --- |
| Profile | 2026-09-20 16:27:04 | 已过期，不能视为正在正常监控 |
| Replies | 2026-09-20 16:27:04 | 已过期，不能视为正在正常监控 |
| Search | 2026-09-21 17:03:34 | 当前仍上报 |

健康接口的两个旧 `state=healthy` 是最后一次上报值，没有随时间衰减更新；业务 `_data_health()` 则会将超过 15 分钟的心跳视为 STALE。这造成健康接口表面正常、Judge 数据陈旧的状态不一致。

停更时间与昨天扩展重载时间接近，旧页面 Content Script 未重新建立连接是可疑原因，但本次未观察页面，不能排除页面关闭、休眠等情况，也不能直接认定用户没有刷新。

上下文任务现为 48 个 COMPLETED、3 个 FAILED，0 个等待/运行。这些是任务完成数，不等同于每条均有完整上下文。

用户关心的真实案例已实际打通父帖与分析：

- 目标回复：`2101352781219258527`。
- 直接父帖：`2101093319501664368`，作者 `udiWertheimer`。
- 任务 420 在 2026-09-20 16:32:18 成功，尝试 1 次；当前上下文 READY。
- 分析 167 在 16:32:30 完成，识别为未来 Banked Reset 预告。
- 候选 3 为 SPECIAL_RESET / BANKED / announced；候选 4 为周二事项指代不明；均 NEEDS_REVIEW。
- 未伪造为已经发卡，也没有新建完整 Reset 周期。

因此旧报告的“17 个上下文任务尚未领取”已是历史快照，不代表当前状态。

## 队列、模型与展示的其他问题

1. 帖子处理任务：233 个完成、13 个失败，当前等待/运行数为 0。pipeline / judge 运行状态均 ready。昨天的超时和 503 曾造成阻塞，但当前不能继续把排队说成这次 UNKNOWN 的直接原因。
2. 现有 Judge 调度仍依赖帖子队列清空；下次模型请求失败并等待重试时，仍可能拖住整体刷新。这是待修复的调度风险。
3. 特殊事件区只读取正式 `special_resets`，不展示待确认预告。即使修好 UNKNOWN，也不会自动出现独立的 Banked 预告卡；不能用创建假完成事件来解决展示缺口。
4. 原有自动化测试通过并不等于这条链通过。既有端到端测试没有覆盖“Judge 真正引用一条上下文回复 → 序列化 → 有效性校验 → API 等级保持”的完整断言，漏掉了本次字段错误。
5. 前次 computer-use 因无法确认浏览器 URL 而停止，仅影响代理的页面核验，不会停止 Backend、扩展或 DeepSeek。持续生成的 Judge 和父帖回执也证明产品未依赖该工具继续运行。

## 修复优先级与本次边界

1. 修复 Judge 读取/校验字段契约，补充引用回复的 API 回归；保留过期、限制和输入变化的真实失效保护。
2. 恢复并核验 Profile / Replies，统一健康状态的时效语义。
3. 明确展示“判断失败、判断失效、采集过期、模型认为未知”，不能全部用一段模糊文案表达。
4. 有界处理队列等待问题，核查 Banked 预告与完整 Reset 判断、候选展示的区别。

本轮按用户“报告状态”的请求执行：没有修改业务代码、没有改库、没有重启、没有主动调用 DeepSeek、没有发送任何通知。只新增本报告；服务保持原状。
