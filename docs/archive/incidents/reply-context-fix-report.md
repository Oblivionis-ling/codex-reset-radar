# 回复父帖自动补全修复 — 2026-09-20

## 结论与验收边界

状态：**PARTIAL_READY / 真实浏览器验收待完成**。长期代码、隔离回归、构建及后端部署已完成；没有收到扩展自动取得真实父帖正文的成功回执，不能宣称全链路通过，也不能确认“周二发 Banked Reset”。原语料裁定和通知配置未修改，无微信或邮件发送。

基线为 `2.0.0-alpha.4`、提交 `53575d0c64525b778dea28ebb919f8d9b1138e98`，本次修复为未提交工作树，不冒充已发布版本。原有启动器修复及通知任务文件保留；未推送、未打标签。

## 真实问题与当前证据链

| 环节 | 已观察结果 |
| --- | --- |
| 目标回复 | `2101352781219258527`，内部帖子 `4647` |
| 修复前 | 已标记回复，但直接父帖 ID、父帖正文均缺失；保存的是中文页面采集文本 |
| 原分析 | 分析 `121` 将独立短句判为 other；Judge `164` 曾引用该回复，却无法取得 Banked 语境 |
| 获取任务 | `REPLY_CONTEXT` 任务 `420`；截至本轮记录仍 PENDING、0 次领取 |
| 父帖 ID、作者、正文 | 尚未取得；不填猜测值 |
| 新分析 | 分析 `151`，`context_sufficient=false`，API 显示 `INSUFFICIENT_INPUT`，不晋升事件 |
| 新分析时间 | `2026-09-20T07:53:53.669322Z`；这次是真实模型处理，但输入仍缺父帖，不是完整上下文验收 |
| 已运行 Judge | 中间构建 `167`、`168`；最终构建 Judge `169` 已完成，加载指纹 `c9721c73c737ef2b` 与源码一致 |
| API / Web | `/api/v2/posts` 返回上下文状态和分析版本；Web 代理与 Backend radar 返回一致；实际浏览器渲染尚未亲眼验证 |

旧页面的特殊事件区域仅展示正式事件，无法凭这条无父帖短回复产生“周二 Banked”的可靠说明。本轮未把未来预告伪造为完成事件，未添加固定颜色或 Tweet ID 特判。

## 正式实现

- 复用 `processing_jobs`，schema 7 兼容迁移保留原任务。新增 `REPLY_CONTEXT` 租约任务及最小上下文节点、版本、实际输入归档映射，不把其他作者塞入 `tibo_posts`。
- 扩展复用已登录 X 环境，只观察正常详情页响应中的直接回复字段；区分父帖、根帖、引用。缓存优先，未命中时串行使用一个不激活的自有辅助标签页；保存归属，用户导航后撤销归属，不操作 Profile/Replies/Search 监控页。
- 最大 3 层祖先、12,000 字符、3 次尝试、90 秒租约、临时错误 300 秒退避；主分析最多等待 100 秒。其他作者只作 context-only，原文不与目标回复拼接。
- 目标正文、关系、上下文正文和可用性形成稳定语义哈希；取得时间与尝试次数不进入处理身份。同样的回执不重复安排分析或 Judge；迟到模型结果不能覆盖新输入。
- 分析、翻译及 Judge 使用现有业务实现与分角色输入；上下文不足阻止无依据事件晋升。已有内容政策继续生效，人工裁定来源即使最初是模型事件也受保护，不覆盖已审字段。
- 历史回放排除判断时刻后取得的上下文，也不以更晚的规范正文编辑替换当时内容。旧分析与旧 Judge 保留。
- 页面仅增加折叠父帖信息、原帖链接、缺失原因和重新分析时间，不改视觉主题。

实际 Prompt：`v2-post-semantics-9-context`、`v2-zh-translation-2-context`、`v2-reset-judge-7-context`。运行健康接口增加已加载核心文件指纹，避免仅用 Git HEAD 判断代码是否生效。

## 回归与数据审计

- Backend：69 项通过（含父帖、版本失效、重复回执、迟到模型结果、已审字段保护、回放时间边界）。隔离 Fake Provider 测试覆盖持久任务 → 正式分析/翻译 → 事件处理 → Judge → API，不冒充真实父帖验收。
- Collector：18 项通过，类型检查和构建通过；覆盖缓存、Worker 重启后标签页归属恢复、用户导航、不跨越内容限制、拒绝任意 URL。
- Web：6 项通过，类型检查和构建通过。
- 生产克隆库迁移两次后保留原 116 个任务，一致性正常。正式源码副本 Backend 69 项通过；未复制生产库、密钥或忽略的临时模块。验证使用现有解释器的依赖环境，并非重新安装依赖。清理命令被执行环境策略拒绝，临时副本仍保留在 `D:\work\20260828-CodexResetRadar\_tmp\reply-clean-cf268091339544348e6e6f2ab15ce858`，不作为产品依赖或提交内容。
- 修复前备份和当前库均 `integrity_check=ok`、外键错误 0。392 条帖子、14 条事件、13 条周期；周期逐行相同，最新完整 Reset 仍为 2026-09-12。
- 初次启用新 Prompt 时，现有 bootstrap 除近期回复外也处理了其原有启动集合：分析行数由 123 增至 160（37 条），不能把全部调用描述为只重跑 17 条回复。未重跑全库。
- 旧自动事件 2、4、5 的 title/summary/provenance/updated_at 有更新；事件类型、时间、范围与周期未变。未删除事件或周期。
- 目前保留 9 个 POST_PROCESSING 失败任务：原有 8 个，新增 1 个空正文翻译失败。最终代码已阻止空正文模型请求以及启动时重复激活同一失败身份；历史失败记录未擦除。

## 尚未完成与恢复点

1. 浏览器控制接口未返回可用浏览器。现有扩展的旧监控心跳仍正常，但 17 个上下文任务未领取；没有证明运行中的扩展已加载新构建。
2. 需要用户在 `edge://extensions` 对现有 Codex Reset Radar 点击一次重新加载，并刷新现有 Profile、Replies、Search 页面。加载目录：`D:\work\20260828-CodexResetRadar\apps\collector-extension\dist`；构建的 `version_name` 为 `2.0.0-alpha.4+reply-context.1`（manifest 数字版本仍 `2.0.0`）。无需卸载或重新绑定账号。
3. 重载后继续从任务 420 等正常队列验收：真实直接父帖回执 → 新语义哈希 → DeepSeek 重分析/翻译 → Judge → API → 实际页面。不得手工贴父帖入库代替成功回执。
4. 当前实现走已存缓存和专用详情页；尚未复用已有普通页面内存中的响应缓存，也不从页面邻接 DOM 猜父子关系。真实 X 重定向、响应形状和 MAIN 观察器仍待现场验证。
5. 已存上下文没有周期性主动刷新机制；重复发现使用缓存。实际新版本回执会使依赖失效，但不能据此宣称已验证自动发现 X 编辑。
6. 父帖译文暂不独立缓存，展示保留原语言。存在已审事件时保守记录差异待复核；受影响的近期回复此前未关联正式事件，因此尚无真实错误自动事件撤回案例。

## 运行与文件

启动/停止使用项目 `.bat` 调用的 Windows PowerShell 路径，本轮验证成功。直接从 PowerShell 7 调 `.ps1` 时，JSON 日期转换曾导致安全校验拒绝停服；未绕过校验、未杀非本项目进程。这一兼容问题保留说明，不混入本轮父帖代码修复。

- Backend：`http://127.0.0.1:8787`，PID `17904`；Web：`http://127.0.0.1:5173`，PID `15204`，结束时保持运行。Profile/Replies/Search 均收到重启后的健康心跳；这不等于新父帖能力已生效。
- 本地原始基线：`D:\work\20260828-CodexResetRadar\runtime\reply-context-fix\before.json`
- 一致性备份：`D:\work\20260828-CodexResetRadar\runtime\reply-context-fix\before.db`
- 迁移验证库：`D:\work\20260828-CodexResetRadar\runtime\reply-context-fix\migration-test.db`
- 最终运行回执：`D:\work\20260828-CodexResetRadar\runtime\reply-context-fix\acceptance.json`
- 实际模型输入：生产库 `reply_context_inputs`；结果：`post_analysis` / `radar_judgements`；事件保留原审计链。

这些本地业务资产均不提交。新增正式文件为 Backend `reply_context.py` 及测试、Collector `context-observer.ts` / `reply-context.ts` 及测试；修改现有采集解析、数据库、任务、Prompt、API、Web 数据绑定与两份架构/数据说明。修复暂不发布，等待真实浏览器验收；微信和邮件真实发送均为 0。
